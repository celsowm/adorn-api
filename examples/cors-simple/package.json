{
  "name": "cors-simple-example",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npx adorn-api build -p tsconfig.json --output .adorn",
    "dev": "tsx server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "adorn-api": "file:../..",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
