{
  "name": "adorn-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && vitest run"
  },
  "dependencies": {
    "express": "^5.2.1",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20.0.0",
    "@types/supertest": "^6.0.3",
    "rimraf": "^6.0.0",
    "supertest": "^7.1.4",
    "typescript": "^5.2.2",
    "vitest": "^4.0.16"
  }
}